import React from 'react';
import PropTypes from 'prop-types';
import '../App.css';

const Error = (props) => {
      return (
          <div className="error">

          </div>
      );
}

Error.propTypes = {
    error: PropTypes.object
};

export default Error;
