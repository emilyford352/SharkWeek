import React from 'react';
import '../App.css';

const Loading = (props) => {
      return (
          <div className="loading">
              Loading...
          </div>
      );
}

export default Loading;
